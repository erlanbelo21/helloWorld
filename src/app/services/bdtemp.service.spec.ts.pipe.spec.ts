import { BdtempServiceSpecTsPipe } from './bdtemp.service.spec.ts.pipe';

describe('BdtempServiceSpecTsPipe', () => {
  it('create an instance', () => {
    const pipe = new BdtempServiceSpecTsPipe();
    expect(pipe).toBeTruthy();
  });
});
